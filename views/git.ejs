<%- include('partials/layout', {
    title: 'Top 10 GIT Commands',
    contentTitle: 'Top 10 GIT Commands',
    contentSubtitle: 'Commands, What They Do, and Examples',
    sections: [
        {
            id: 'git-commands',
            title: 'Common GIT Commands',
            type: 'table',
            tableCaption: 'Common GIT Commands',
            tableHeaders: ['Command', 'Description', 'Example'],
            tableRows: [
                ['<code>git init</code>', 'Initializes a new Git repository in the current directory', '<code>git init</code> - Initializes a Git repository in the current directory, creating a <code>.git</code> folder'],
                ['<code>git clone</code>', 'Copies a repository from a remote server to your local machine', '<code>git clone https://github.com/user/repo.git</code> - Clones a remote repository located at the URL to your local machine'],
                ['<code>git add</code>', 'Stages changes to be committed', '<code>git add file.txt</code> - Stages a specific file'],
                ['<code>git commit</code>', 'Commits staged changes to the repository', '<code>git commit -m "Initial commit"</code> - Commits with a message'],
                ['<code>git push</code>', 'Pushes local changes to the remote repository', '<code>git push origin main</code> - Pushes changes to the main branch'],
                ['<code>git pull</code>', 'Fetches and merges changes from the remote repository', '<code>git pull origin main</code> - Pulls updates from the main branch'],
                ['<code>git status</code>', 'Shows the status of changes in the working directory', '<code>git status</code> - Displays staged, unstaged, and untracked changes'],
                ['<code>git branch</code>', 'Lists, creates, or deletes branches', '<code>git branch -a</code> - Lists all branches'],
                ['<code>git checkout</code>', 'Switches branches or restores files', '<code>git checkout feature-branch</code> - Switches to the feature-branch'],
                ['<code>git merge</code>', 'Merges another branch into the current branch', '<code>git merge develop</code> - Merges the develop branch into the current branch']
            ]
        }
    ]
}) %>